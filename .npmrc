# Windows-friendly pnpm settings to avoid deep paths (CMake object path limit).
# See: android build failures mentioning CMAKE_OBJECT_PATH_MAX / 250 chars.

# Use a classic node_modules layout (avoids long nested symlink paths).
node-linker=node-modules

# Keep pnpm's virtual store in a *very* short folder at the repo root instead of node_modules/.pnpm
# (significantly reduces absolute path length during native builds).
virtual-store-dir=.p


